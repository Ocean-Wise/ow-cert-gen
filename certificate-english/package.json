{
  "scripts": {
    "deploy": "now --public && now alias && now rm $npm_package_now_name --safe --yes"
  },
  "now": {
    "name": "ow-english-certificate",
    "alias": [
      "ow-english-certificate"
    ]
  }
}
